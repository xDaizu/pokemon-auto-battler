node_modules/
dist/
dist-ssr/
*.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.log

.env
.env.*
!.env.example

# Firebase CLI deploy cache (hashes of uploaded files)
.firebase/

# Names the real GCP/Firebase project id — see .firebaserc.example
.firebaserc

# Production SPA build output (frontend/vite.config.ts emits into
# hosting/battler/, the Firebase Hosting public dir)
hosting/

# Local libSQL/SQLite database (plus its -wal/-shm journal siblings)
local.db*

.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
